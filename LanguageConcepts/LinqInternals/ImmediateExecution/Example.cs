namespace InterviewPreparation.LanguageConcepts.LinqInternals.ImmediateExecution;

public class Program
{
    /* public static void Main()
    {
        var users = new Dictionary<string, int>()
        {
            { "Mike", 21 },
            { "Jaden", 18 },
            { "Maria", 16 },
            { "Vale", 25 }
        };

        var minorUsers = users.Where(u => u.Value < 18)
                          .ToDictionary(u => u.Key, u => u.Value);

        users.Add("Paul", 17);

        foreach (var user in minorUsers)
        {
            Console.WriteLine(user.Key);
        }
    } */
}