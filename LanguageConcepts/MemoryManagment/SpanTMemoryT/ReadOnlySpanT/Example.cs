namespace InterviewPreparation.LanguageConcepts.MemoryManagment.ReadOnlySpanT;

public class Program
{
    /* public static void Main()
    {
        string str = "Hello, Wordl!";

        ReadOnlySpan<char> substr = str.AsSpan(7, 5);

        foreach (var x in substr)
        {
            Console.WriteLine(x);
        }
    } */
}