namespace InterviewPreparation.LanguageConcepts.MemoryManagment.SpanTMemoryT.SpanT;

public class Program
{
    /* public static void Main()
    {
        int[] arr = [1, 2, 3, 4, 5];

        Span<int> slice = arr.AsSpan(2, 3);
        slice[0] = 9;
        
        foreach(var x in slice)
        {
            Console.WriteLine(x);
        }
    } */
}